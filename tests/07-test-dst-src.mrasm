#!mrasm

ADDR:
    .DB 42
    .DB ADDR
    LD R0, ADDR

LOOP:
    DEC R0
    INC R0
    DEC (R0)
    DEC (R0+)
    DEC ((R0+))
    DEC 0xFF
    DEC (0xFF)
LOOP:
    INC R0
    JZ LOOP
    JR FIRST_LINE
